# Dotfiles

## Usage

Clone the repo:

```sh
git clone https://github.com/knealking/dotfiles/tree/windows dotfiles
```

## Setup for Windows

```sh
cd dotfiles
git switch windows
```

Apply the config from windows config
